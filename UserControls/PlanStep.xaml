<UserControl x:Class="WorkoutGenerator.UserControls.PlanStep"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkoutGenerator.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Content="{Binding NumberInList}"/>
        <Label Grid.Column ="1" Content="{Binding Exercise.Name}"/>
        <Label Grid.Column="2">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Exercise.TargetArea}"/>
                <TextBlock Text="/"/>
                <TextBlock Text="{Binding Exercise.Type}"/>
            </StackPanel>
        </Label>
        <TextBox MaxLength="100" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Name="txtLimitedInput" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />
    </Grid>
</UserControl>
